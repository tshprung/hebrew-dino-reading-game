package com.tal.hebrewdino.ui.domain

/** Plain-word captions for rhyming-station cards (catalog [word] is also plain for audio/validation). */
object Season2RhymingDisplayWords {
    fun displayWord(catalogId: String, plainWord: String): String = plainWord
}
